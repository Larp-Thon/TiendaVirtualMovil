<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Permiso de cámara -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TiendaOnline">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
            
        <!-- Proveedor de archivos para compartir la foto de la cámara con la app -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".presentation.contacto.Contacto"
            android:exported="false"
            android:label="@string/title_activity_contacto"
            android:theme="@style/Theme.TiendaOnline" />
        <activity
            android:name=".presentation.carrito.CarritoActivity"
            android:exported="false"
            android:label="@string/title_activity_carrito"
            android:theme="@style/Theme.TiendaOnline" />
        <activity
            android:name=".presentation.producto.CrearProductoActivity"
            android:exported="false"
            android:label="@string/title_activity_crear_producto"
            android:theme="@style/Theme.TiendaOnline" />
        <activity
            android:name=".MainMenuActivity"
            android:exported="false"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/Theme.TiendaOnline" />
        <activity
            android:name=".presentation.registro.RegistroActivity"
            android:exported="false"
            android:label="@string/title_activity_registro"
            android:theme="@style/Theme.TiendaOnline" />
        <activity
            android:name=".presentation.perfil.PerfilActivity"
            android:exported="false"
            android:label="@string/title_activity_perfil"
            android:theme="@style/Theme.TiendaOnline" />
        <activity
            android:name=".presentation.login.LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.TiendaOnline" />
        <activity
            android:name=".presentation.bienvenida.BienvenidaActivity"
            android:exported="false"
            android:label="@string/title_activity_bienvenida"
            android:theme="@style/Theme.TiendaOnline" />
        <activity
            android:name=".presentation.splash.SplashActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TiendaOnline">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.TiendaOnline" />
    </application>

</manifest>